# Session Log — 2026-06-20

## Session — 2026-06-20 05:46

### Built / Changed
- Created plan file `C:\Users\shriy\.claude\plans\explain-how-langgraph-llamaindex-structured-grove.md` — full architecture explanation of LangGraph + LlamaIndex FAISS + litellm in the AI Advisor

### Decisions
- **LangGraph orchestrates a 6-node DAG** (Router → Specialist → KnowledgeAgent → Synthesizer → Guardrails) with shared `AgentState` TypedDict; no inter-node messaging
- **Router uses claude-haiku-4-5** (73% cheaper) for intent classification only; Specialists and Synthesizer use claude-sonnet-4-6
- **KnowledgeAgent and Guardrails have no LLM calls** — pure FAISS retrieval + NetworkX traversal + Python validation (zero token cost)
- **LlamaIndex FAISS uses top-k=3** (not 5) — saves ~40% context tokens with no measurable quality loss (ADR 006)
- **FACES edge in GraphRAG** carries `lambda_posterior` + `goals_conceded_pg` updated by `update_round_fdr()` post-round — captaincy advice grounded in live defensive form
- **litellm Router** provides automatic fallback: Claude Sonnet → Azure GPT-4o → Vertex Gemini → BFF legacy handler; "least-busy" routing strategy
- **Prompt caching** splits system prompt into Block 1 (cached WC rules ~2048 tokens) + Block 2 (fresh RAG/graph context per request)
- **Elite Product Team verdict**: xP model is sound but signal degrades at every downstream surface; highest-leverage fixes are (1) run `update_round_fdr()` now, (2) add `canAddPlayer()` guard to `transferAdvisor.ts`, (3) inject real xP into `/api/chat` system prompt immediately

### Dead Ends
- No implementation attempted this session — exploration and architecture explanation only

### What's Next
- **Run `update_round_fdr()` after Round 2 completes** — populates `goals_pg`/`goals_conceded_pg`, fixes all FDR-downstream recommendations (currently NULL, seed-based FDR is stale)
- **Add `canAddPlayer()` guard to `transferAdvisor.ts`** — eliminates country limit bug (4th player from same country can currently be suggested)
- **Inject top-10 xP + FDR into `/api/chat` system prompt** — grounds AI advisor with real data at zero architecture cost before Phase 1 LangGraph ships
- **Phase dynamic country cap into MILP optimizer** — currently fixed at 3 per team regardless of round (should be 3→4→5→6 through R16/QF/SF/Final)
- **Phase 1 AI Advisor** — build `services/ai-advisor/`: LangGraph StateGraph, LlamaIndex FAISS indexer, litellm router, FastAPI + SSE `/chat`, BFF circuit-breaker proxy

## Session — 2026-06-20 18:37

### Built / Changed
- Renamed `_fetch_group_results()` → `_fetch_all_results()` in `engine/engine/wc_model.py` — now reads ALL rounds with `status='complete'` instead of filtering on `stage=='GROUP'`
- Removed `post_group` gate from Bayesian FDR update in `run_model()` — update now runs automatically on every engine call when actual match data exists
- Added `status` field to rounds SQL query in `run_model()` so non-complete rounds can be correctly targeted for Bayesian update
- Fixed `fdr_rows` in `run_model()` — `goals_pg` and `goals_conceded_pg` now populated from actual match results (were NULL/placeholder before)
- Added `update_round_fdr(conn)` to `wc_model.py` — standalone DB UPDATE for `goals_pg`, `goals_conceded_pg`, `xgc_pg`, `def_multiplier` without full model rerun
- Wired `update_round_fdr()` into `wc_run.py` pipeline (called after `blend_live_observations()`)
- Updated `engine/tests/test_model.py`: renamed `TestFetchGroupResults` → `TestFetchAllResults`, updated test fake data to use `status` field, added new `test_accumulates_across_multiple_complete_rounds` test case
- All 50 pytest pass (was 49 before — one new test added)

### Decisions
- **Always run Bayesian FDR update** (not gated on `--post-group` flag) — every engine run now uses latest actual data; `post_group` param kept for backward compat but is a no-op
- **Filter on `status='complete'`** not `stage=='GROUP'` — generalises to all rounds (R32, R16, QF, SF, Final) automatically
- **`update_round_fdr()` as standalone function** — allows fast DB refresh without regenerating full projections; called at end of every pipeline run
- **`goals_pg`/`goals_conceded_pg` sourced from `_fetch_all_results()`** in same `run_model()` call — no extra HTTP request

### What's Next
- Add `canAddPlayer()` guard to `transferAdvisor.ts` — eliminates country limit bug in transfer suggestions
- Inject top-10 xP + FDR into `/api/chat` system prompt — grounds AI advisor with live data
- Phase dynamic country cap into MILP optimizer (3→4→5→6 by round)
- Phase 1 AI Advisor (`services/ai-advisor/`): LangGraph, LlamaIndex FAISS, litellm router, SSE
