# Session Log — 2026-06-22

## Session — 2026-06-22 14:07

### Built / Changed
- Generalised `_fetch_group_results()` → `_fetch_all_results()` in `engine/engine/wc_model.py` — filters on `status='complete'` across all round stages
- `run_model()` always applies Bayesian FDR update when actual data exists; `post_group` flag is now a no-op kept for backward compat
- Added `status` to rounds SQL query so non-complete rounds are correctly targeted
- Fixed `fdr_rows` — `goals_pg` and `goals_conceded_pg` now populated from actual match results (were NULL/seed-placeholder before)
- Added `update_round_fdr(conn)` to `wc_model.py` — standalone DB UPDATE for `goals_pg`, `goals_conceded_pg`, `xgc_pg`, `def_multiplier`
- Wired `update_round_fdr()` into `wc_run.py` pipeline after `blend_live_observations()`
- Updated `engine/tests/test_model.py`: renamed `TestFetchGroupResults` → `TestFetchAllResults`, updated fake data to use `status` field, added `test_accumulates_across_multiple_complete_rounds`
- Committed in two batches (engine, then docs) and pushed to `origin/main` (`edc25e3`)

### Decisions
- **Two commits** (engine first, docs second) — user preference for clean separation
- **`post_group` kept as no-op** — backward compat with `wc_run.py` `--post-group` flag and `engine.yml` workflow dispatch input

### What's Next
- Add `canAddPlayer()` guard to `transferAdvisor.ts` — eliminates country limit bug in transfer suggestions
- Inject top-10 xP + FDR into `/api/chat` system prompt — grounds AI advisor with live data at zero architecture cost
- Phase dynamic country cap into MILP optimizer (3→4→5→6 by round)
- Phase 1 AI Advisor (`services/ai-advisor/`): LangGraph StateGraph, LlamaIndex FAISS, litellm router, SSE

## Session — 2026-06-22 15:32

### Built / Changed
- `web/server/services/transferAdvisor.ts`: added `maxPerCountry = 3` param; tracks `countryCounts` map across sequential swaps; skips `inCard` from a different team that would breach the cap; updates counts after each applied transfer
- `web/server/server.ts`: `POST /api/transfers/suggest` now fetches rounds, derives `roundPhase` + `COUNTRY_LIMIT[phase]`, passes correct limit to `suggestTransfers` (R16=4, QF=5, SF=6, Final=8)
- `web/server/server.ts`: `/api/chat` handler now fetches top-10 xP from live DB projections and injects as `<live_data>` into the Haiku system prompt; non-fatal if DB is down
- `engine/engine/wc_optimizer.py`: added `_STAGE_COUNTRY_CAP` dict and `_country_cap(stage)` helper; `run_optimizer` queries round stage from DB and applies dynamic country cap instead of hardcoded `MAX_PER_TEAM=3`; `differential` variant stays fixed at 2

### Decisions
- Country limit check skips when `inCard.team_abbr == outCard.team_abbr` — swapping within same team is always safe regardless of current count; initial check was wrong and failed existing tests
- `differential` variant keeps hardcoded cap of 2 — intentionally tighter than any phase limit to force squad diversity
- Live xP injected at end of system prompt string (not a separate cache block) — squad names already make the prompt per-user so cache hit was never reliable anyway

### What's Next
- Phase 1 AI Advisor (`services/ai-advisor/`): LangGraph StateGraph, LlamaIndex FAISS, litellm router, SSE
- ADR 002 server-side: extend `canAddPlayer` enforcement to `/api/transfers/suggest` response validation
- Captain mid-round: add fuzzy/abbr fallback for ESPN team-name matching if FT detection misfires in prod
